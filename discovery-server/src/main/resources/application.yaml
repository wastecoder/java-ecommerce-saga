spring:
  application:
    name: discovery-server
  cloud:
    service-registry:
      auto-registration:
        enabled: false

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
