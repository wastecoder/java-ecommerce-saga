spring:
  # The test application.yaml shadows src/main (Spring Boot loads classpath:/application.yaml from the
  # first classpath root only), so the Kafka serde config that production needs is mirrored here. The
  # producer block is needed because the dead-letter recoverer publishes through the auto-configured
  # producer, and the integration tests publish envelopes to order.events with it.
  kafka:
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JacksonJsonSerializer
      acks: all
      retries: 3
      properties:
        enable.idempotence: true
        max.in.flight.requests.per.connection: 5
        spring.json.add.type.headers: false
    consumer:
      group-id: notification-service
      auto-offset-reset: earliest
      key-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      properties:
        spring.deserializer.key.delegate.class: org.apache.kafka.common.serialization.StringDeserializer
        spring.deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JacksonJsonDeserializer
        spring.json.use.type.headers: false
        spring.json.value.default.type: com.wastecoder.shopflow.notification.adapter.messaging.EventEnvelope
        spring.json.trusted.packages: com.wastecoder.shopflow.notification.adapter.messaging

eureka:
  client:
    enabled: false

problem-details:
  base-uri: https://wastecoder.com/problems
