# 📦 Progresso — ShopFlow (MVP + Notification)

Roadmap em fases para construir o MVP. A sequência **fecha a saga cedo** e vai agregando qualidade.

> **Como usar:** ao concluir um item, marque a caixa **e** risque o texto:
> `- [x] ~~item concluído~~`. Mantenha este arquivo sempre em dia. A visão "Completo" (pós-MVP)
> está em `FUTURE-PROGRESS.md`.

## 🧱 Fase 0 — Fundação do monorepo
- [x] ~~Criar monorepo Gradle (Kotlin DSL): `settings.gradle.kts` + `build.gradle.kts` raiz (Java 21, BOMs, jacoco, pitest)~~
  - **6 projetos gerados no Spring Initializr** (Gradle-Kotlin, JAR, YAML, **Java 21**, **Boot 4.0.7**, **Spring Cloud 2025.1.1 "Oakwood"**, group `com.wastecoder.shopflow`) montados num **multi-projeto Gradle**: `settings.gradle.kts` inclui os 6 subprojetos; wrapper **Gradle 9.5.1** promovido à raiz; docs em `doc/`; estrutura achatada (sem sobras de zip — `HELP.md`/`compose.yaml`/settings duplicados removidos).
  - **`build.gradle.kts` raiz** centraliza tudo via `subprojects {}`: group/version, **toolchain Java 21**, `mavenCentral()`, BOM do Spring Cloud por serviço e as versões num só lugar (Boot 4.0.7, dependency-management 1.1.7, **MapStruct 1.6.3** + `lombok-mapstruct-binding` 0.2.0, **springdoc 3.0.3**, plugin Pitest 1.19.0 / core 1.25.4 / junit5-plugin 1.2.3). Cada subprojeto usa `plugins {}` **sem versão**.
  - **Convenção de testes** (espelha o `partners-api`): a task `test` roda só unitários (exclui `*IntegrationTest`, **sem Docker**) e uma task `integrationTest` separada roda os de integração com **Testcontainers** (Kafka + PostgreSQL); `failOnNoDiscoveredTests=false` (Gradle 9). Gates ligados ao `check`: **JaCoCo** BUNDLE (LINE ≥ 0.85 / BRANCH ≥ 0.75) e **Pitest** (mutators STRONGER, thresholds 80).
  - **Dependências fora do Initializr** adicionadas à mão: MapStruct (+`mapstruct-processor` +`lombok-mapstruct-binding`, ordem dos annotation processors lombok → binding → mapstruct), **springdoc-openapi** v3 (linha do Boot 4) nos 4 serviços de domínio, e os plugins **JaCoCo + Pitest** na raiz. (`spring-kafka-test` já veio do Initializr.)
  - **`application.yaml` por serviço**: `spring.application.name` + porta do §5 + `eureka...defaultZone` apontando p/ `:8761`. `discovery-server` é Eureka **standalone** (`auto-registration.enabled: false` + `register-with-eureka`/`fetch-registry: false`). Em `src/test` cada serviço recebe `eureka.client.enabled: false` (silencia conexão recusada).
  - **Testes gerados renomeados** para `*IntegrationTest` (`@Import(TestcontainersConfiguration)` + `@SpringBootTest`) com `@DisplayName` Given/When/Then — assim `gradlew test` roda sem Docker e o `contextLoads` com containers cai na `integrationTest`.
  - **Verificado:** `gradlew build` **verde** — 2 testes unitários (`api-gateway`, `discovery-server`) + 4 `contextLoads` subindo **Kafka + PostgreSQL reais** via Testcontainers (0 falhas); gates JaCoCo passam (módulos ainda sem código de produção); smoke do `pitest` resolve as versões. **Sem `git init`/commits** nesta etapa.
  - **Caveats / fora deste item:** `gradlew build` e `check` **exigem Docker** (o `check` encadeia `integrationTest` — é o critério da Fase 4). **Pitest × JUnit 6**: o `pitest-junit5-plugin` 1.2.3 ainda não suporta o JUnit 6 do Boot 4 oficialmente — confirmar "Ran N tests > 0" quando houver testes de verdade (Fase 4). Os próximos itens da Fase 0 (`docker-compose.yml` + `infra/init-db.sql`, `@EnableEurekaServer`, rota dummy do gateway) são **código**.
- [x] ~~`docker-compose.yml` com Kafka (KRaft) + Kafka UI + PostgreSQL + `infra/init-db.sql`~~
  - **`docker-compose.yml` na raiz** (`name: shopflow`, rede `shopflow`) com 3 serviços: **`postgres`** (`postgres:17`, healthcheck `pg_isready`, volume `postgres-data`), **`kafka`** (`apache/kafka-native:4.3.0`, **KRaft single-node** broker+controller, storage efêmero — a imagem roda como non-root e não escreve em volume nomeado root-owned; dev não precisa persistir o log) e **`kafka-ui`** (`kafbat/kafka-ui`, sucessor mantido do provectus) em **`:8085`** apontando p/ o cluster `shopflow`.
  - **Kafka dual-listener**: `INTERNAL://kafka:29092` (containers, ex.: kafka-ui) e `EXTERNAL://localhost:9092` (host / `bootRun`), + `CONTROLLER` interno. Replication factor 1 (dev). Sem healthcheck no kafka porque a imagem `*-native` é mínima (sem `bin/*.sh`); o kafka-ui já tem retry próprio (`depends_on: service_started`).
  - **`infra/init-db.sql`** cria os 4 bancos **database-per-service** (`orderdb`/`inventorydb`/`paymentdb`/`notificationdb`) — roda só na **primeira** init do volume (`/docker-entrypoint-initdb.d`).
  - **`.env.example`** (versionado) + **`.env`** (local, agora no `.gitignore` com `!.env.example`) com credenciais/portas; o compose usa `${VAR:-default}`, então sobe sem `.env`.
  - **Fora deste item:** os **tópicos** (`inventory.commands`, `payment.commands`, `order.events`, `inventory.events`, `payment.events` + `*.DLT`) virão de `KafkaTopicsConfig` (`NewTopic`) na **Fase 2**. O wiring de **datasource por serviço** + a interação do `spring-boot-docker-compose` no `bootRun` são da **Fase 1**.
- [x] ~~`discovery-server` (Eureka) e `api-gateway` com uma rota dummy~~
  - **`discovery-server`**: `@EnableEurekaServer` na app class (config standalone já existia — `:8761`, `register-with-eureka`/`fetch-registry: false`). Dashboard em `http://localhost:8761`.
  - **`api-gateway`**: rota dummy via **bean `RouteLocator` em Java** (`config/GatewayRoutesConfig`) — `/dummy/**` → `StripPrefix=1` → **`lb://api-gateway`** (encaminha ao próprio actuator). Adicionado **`spring-cloud-starter-loadbalancer`** (necessário p/ `lb://` no gateway 2025.x). Actuator `gateway` exposto + habilitado (`management.endpoint.gateway.access: unrestricted`, sintaxe Boot 4).
  - **Verificado:** `:discovery-server:test` + `:api-gateway:test` verdes (2 `contextLoads`, sem Docker). Smoke manual: `API-GATEWAY` registrado no Eureka; `GET /dummy/actuator/health` → **200 `{"status":"UP"}`** (gateway resolveu o serviço via Eureka — critério da fase); `GET /actuator/gateway/routes` lista a `dummy-route`.
  - **Fora deste item:** rotas reais p/ `order-service`/`inventory-service`, filtros (rate limit, JWT), circuit breaker → Fases 1/2/6.
- **Critério:** ~~`docker compose up` sobe a infra e o gateway resolve um serviço via Eureka.~~ ✅ **Fase 0 concluída.**

## 🧩 Fase 1 — Order & Inventory (REST + JPA, hexagonal horizontal)
- [x] ~~`order-service`: domínio (Order/OrderItem/OrderStatus), portas, use cases, Flyway, `POST/GET /orders`~~
  - **Hexagonal horizontal** espelhando o `partners-api`. **Domínio**: records `Order`/`OrderItem` (Bean Validation) + factory `Order.place(...)` que **congela** o `unitPrice` do request e calcula `totalAmount`; enum `OrderStatus` (6 valores do §7, só `PENDING` usado); `DomainException` + `OrderNotFoundException`.
  - **Aplicação**: portas `PlaceOrderUseCase`/`GetOrderUseCase` (in), `OrderRepository` (out), `PlaceOrderCommand`; impls `@Service`/`@Transactional`, validação via `Validator`.
  - **Web**: `OrderController` (`POST /orders` → 201+`Location`; `GET /orders/{id}` → 200), DTOs record com `toCommand()`/`from()`, `GlobalExceptionHandler` (`@RestControllerAdvice`) + `ProblemType` (**RFC 7807**).
  - **Persistência**: `OrderEntity`/`OrderItemEntity` (JPA+Lombok, `@OneToMany`), `OrderJpaDatabase`, `OrderRepositoryImpl` (`@Transactional` p/ inicializar a coleção lazy ao mapear), **`OrderEntityMapper` (MapStruct** + `@AfterMapping` p/ back-ref). Flyway `V1__create_order_tables.sql`.
  - **Config**: datasource → `orderdb` (database-per-service; `spring.docker.compose.enabled: false`, dev sobe a infra com `docker compose up -d postgres`); `ddl-auto: validate`.
  - **Decisão de preço:** `unitPrice` por **snapshot no request** no MVP; `catalog-service` como autoridade de preço fica para o FUTURE (Fase 11). O `StockItem` (§7) segue só com disponibilidade.
  - **Verificado:** `:order-service:build` **verde** — unit (domínio/use cases/`@WebMvcTest`/mapper/ProblemType) + `OrderRepositoryImplIntegrationTest` (Testcontainers, valida Flyway); **JaCoCo LINE 0.96 / BRANCH 0.96**. Smoke manual: `POST /orders` → 201 (`PENDING`, `totalAmount` somado), `GET` → 200, id aleatório → 404 ProblemDetail.
- [x] ~~`inventory-service`: domínio (StockItem/StockReservation), `GET /stock`, seed inicial~~
  - **Hexagonal horizontal** (mesma convenção do order: `adapter.web.handler` + `adapter.persistence.{entity,mapper,database}`). **Domínio**: record `StockItem(productId, available, reserved)` (`productId` = identidade) + `DomainException`/`StockItemNotFoundException`. **`StockReservation` adiado p/ a Fase 2** (ver item abaixo).
  - **Aplicação**: `ListStockUseCase`/`GetStockUseCase`/`SeedStockUseCase` (in), `StockRepository` (out), `SeedResult`; impls `@Service`/`@Transactional`.
  - **Web**: `StockController` (`GET /stock` → lista; `GET /stock/{productId}` → 200/404), `StockResponse`, `GlobalExceptionHandler` + `ProblemType` (RFC 7807, inclui `STOCK_ITEM_NOT_FOUND`).
  - **Persistência**: `entity/StockItemEntity`, `mapper/StockItemEntityMapper` (MapStruct), `database/StockItemJpaDatabase`, `StockRepositoryImpl`. Flyway `V1__create_stock_tables.sql` (só `stock_items`).
  - **Seed inicial** (estilo `partners-api`): `adapter/seed/StockSeederRunner` (`@Profile("seed")` + `CommandLineRunner`) lê `seed/sample-stock.json` (4 product UUIDs fixos `a1..a4`) → `SeedStockUseCase` **idempotente**. `spring.profiles.active: seed` no dev; `test` nos testes.
  - **Config**: datasource → `inventorydb`; `ddl-auto: validate`; `docker.compose.enabled: false`.
  - **Verificado:** `:inventory-service:build` **verde** — unit + `StockRepositoryImplIntegrationTest` + `StockSeederIntegrationTest` (`@ActiveProfiles("seed")`, Testcontainers); **JaCoCo LINE 0.97 / BRANCH 1.00**. Smoke: `GET /stock` → 200 com os 4 itens semeados; `GET /stock/{id}` → 200; id aleatório → 404 ProblemDetail.
- [x] ~~Problem Details (RFC 7807) + `GlobalExceptionHandler`~~
  - Implementado em `order-service` e `inventory-service` (`adapter.web.handler` + `ProblemType` por serviço). Replicar nos próximos serviços.
- **Critério:** criar pedido (`PENDING`) e consultar estoque via REST (sem Kafka ainda).

## 🔄 Fase 2 — Kafka + SAGA orquestrada (núcleo)
- [x] ~~Configurar tópicos, produtor idempotente e consumidores~~
  - **Fundação Kafka (infra) nos serviços do núcleo da Fase 2: `order-service` e `inventory-service`.** Cada serviço ganhou (duplicado, sem módulo compartilhado — mesma convenção do `ProblemType`): `adapter/config/KafkaTopicsConfig` (beans `NewTopic` via `TopicBuilder`), `adapter/messaging/EventEnvelope` (o envelope de fio), porta `application/port/out/EventPublisher` e o adapter `adapter/messaging/out/KafkaEventPublisher`.
  - **Tópicos — produtor é dono** (3 partições, RF 1 no broker KRaft single-node): `order-service` declara `inventory.commands`, `payment.commands`, `order.events`; `inventory-service` declara `inventory.events`. O `KafkaAdmin` auto-configurado do Boot cria os tópicos no startup. `payment.events` nasce com o `payment-service` (Fase 3).
  - **Produtor idempotente** (config em `application.yaml`, auto-config do Boot — sem factory à mão): `acks=all`, `enable.idempotence=true`, `max.in.flight<=5`, key=`StringSerializer`. **Chave = orderId** em todo publish → ordenação por pedido (mesma partição).
  - **Serde JSON desacoplado entre serviços:** `JacksonJsonSerializer`/`JacksonJsonDeserializer` (o `JsonSerializer`/`JsonDeserializer` está `@Deprecated(forRemoval)` no Spring Kafka 4) com **type headers desligados** (`spring.json.add.type.headers=false` no produtor; `spring.json.use.type.headers=false` + `value.default.type=<EventEnvelope local>` + `trusted.packages` no consumidor). Envelope `{ eventId, type, orderId, occurredAt, payload }` com `payload` como `Object` (o consumidor recebe um `Map`; itens 2/3 convertem para DTO conforme `type`). **Consumidor pronto** (factory default auto-configurada): os itens 2/3 só adicionam `@KafkaListener`, sem mais fiação.
  - **Verificado:** `:order-service:check` + `:inventory-service:check` **verdes** — unit (`KafkaEventPublisherTest`, Mockito) + integração Testcontainers (`KafkaEnvelopeRoundTripIntegrationTest`: publica via `EventPublisher` e consome com `@KafkaListener` só-de-teste, afirmando chave=orderId, round-trip completo do envelope/payload e **ausência do header `__TypeId__`**); **JaCoCo** (LINE/BRANCH) e **Pitest** (10/10 mutações, 100%) passam. Stack confirmada: Spring Boot 4.0.7, Spring Kafka 4.0.6, kafka-clients 4.1.2.
  - **Caveat (test config):** o `src/test/resources/application.yaml` **sombreia** o de `src/main` (o Boot carrega `classpath:/application.yaml` só da primeira raiz do classpath), então o bloco `spring.kafka` foi **espelhado no yaml de teste** — mesmo motivo pelo qual o `problem-details.base-uri` já era duplicado lá.
  - **Fora deste item:** `@KafkaListener` concretos + `OrderSagaCoordinator` (item 2); `StockReservation` + consumo de `inventory.commands` (item 3); `processed_messages` + `DeadLetterPublishingRecoverer`/tópicos `*.DLT` (item 4); infra Kafka de `payment-service`/`notification-service` (Fase 3).
- [x] ~~`OrderSagaCoordinator`: `ReserveStock → ProcessPayment → Confirm` e compensação `ReleaseStock`~~
  - **`OrderSagaCoordinator`** (`application/usecase`) implementa as in-ports `HandleStockReplyUseCase`/`HandlePaymentReplyUseCase`; orquestra carregando o pedido, aplicando uma **transição de domínio** e publicando o próximo passo. Dois `@KafkaListener` enxutos (`adapter/messaging/in`: `StockReplyListener` em `inventory.events`, `PaymentReplyListener` em `payment.events`) roteiam por `envelope.type()` usando o `orderId` do topo do envelope.
  - **Transições no agregado `Order`** (imutável, retornam novo `Order` ou lançam `InvalidOrderStateException`): `markStockReserved` (PENDING→STOCK_RESERVED), `markPaid` (STOCK_RESERVED→PAID), `confirm` (PAID→CONFIRMED), `reject` (PENDING→REJECTED), `cancel` (STOCK_RESERVED→CANCELLED). `PAID` é **transitório** dentro do handler de `PaymentAuthorized` (`markPaid().confirm()`), persistindo só `CONFIRMED`.
  - **Fluxo:** `PlaceOrder` persiste PENDING, emite **`OrderCreated`** (`order.events`) e o comando **`ReserveStock`** (`inventory.commands`). `StockReserved`→`ProcessPayment`. `PaymentAuthorized`→CONFIRMED + `OrderConfirmed`. `PaymentFailed`→**compensação** `ReleaseStock` + CANCELLED + `OrderCancelled`. `StockReservationFailed`→**REJECTED + `OrderRejected`** (corrigido o lapso do §6 do CHALLENGE, que dizia `OrderCancelled`). `StockReleased` é ack/no-op observável.
  - **Portas de saída de domínio** `OrderCommandPublisher`/`OrderEventPublisher` (conforme §9) sobre o `EventPublisher` genérico do item 1 (reaproveitado); a aplicação não conhece tópicos nem tipos. Nomes de tópicos e tipos centralizados em `adapter/messaging/Topics` e `MessageType`.
  - **Idempotência parcial via guardas de estado:** réplicas duplicadas/fora-de-ordem/estado terminal lançam `InvalidOrderStateException` → o coordinator registra e ignora (sem save, sem publish); `orderId` inexistente e tipo desconhecido também são ignorados (commitam o offset). Dedup real (`processed_messages`) + DLT ficam no **item 4**. Falha de publish propaga (redelivery). DB+Kafka não-atômicos (Outbox = FUTURE).
  - **Verificado:** `:order-service:check` **verde** + **Pitest 100%** (23/23 mutações; coordinator e `PlaceOrderUseCaseImpl` são alvos). Unit (coordinator 13, transições 10, listeners 4+3, place 2) + integração Testcontainers `OrderSagaIntegrationTest` (4 cenários) que **simula** estoque/pagamento (publicando respostas falsas via o próprio `EventPublisher`) e cobre confirma / sem-estoque-rejeita / pagamento-recusado-cancela+compensa / **redelivery idempotente**.
  - **Fora deste item:** consumidor de `inventory.commands` + domínio de reserva (**item 3**); `payment-service` (**Fase 3**) — ambos **simulados** nos testes; `processed_messages` + DLT (**item 4**).
- [x] ~~**inventory: `StockReservation`/`ReservationStatus` + tabela `stock_reservations` + casos de uso ReserveStock/ReleaseStock** (adiado da Fase 1; nasce junto do consumidor `inventory.commands`)~~
  - **Domínio**: record imutável `StockReservation(id, orderId, productId, quantity, status)` (§7) com factories `reserve(...)`→RESERVED e `failed(...)`→FAILED e transição `release()` (RESERVED→RELEASED; ilegal lança `IllegalReservationStateException`); enum `ReservationStatus {RESERVED, RELEASED, FAILED}`. O record `StockItem` ganhou comportamento `canReserve/reserve/release` (lança `InsufficientStockException`/`IllegalStockReleaseException`). Domínio sem Spring/JPA.
  - **Aplicação**: in-ports `ReserveStockUseCase`/`ReleaseStockUseCase` (`execute(StockCommand)`); out-ports `StockReservationRepository` (+`findByOrderId`/`findByOrderIdAndStatus`) e `InventoryEventPublisher` (porta de domínio espelhando o `OrderEventPublisher`); viewmodel `StockCommand(orderId, items)`. Impls `@Service`/`@Transactional` que **publicam a própria resposta** (listener fino, como o coordinator do order).
  - **ReserveStock all-or-nothing**: valida todos os itens antes de aplicar — sucesso decrementa o estoque + grava reservas RESERVED + `StockReserved`; falta/inexistente grava reservas **FAILED** (trilha de auditoria, **sem** mexer no estoque) + `StockReservationFailed`. **ReleaseStock** usa as reservas RESERVED persistidas como fonte da verdade (estoque volta ao valor original, reservas→RELEASED, `StockReleased`); ack idempotente se não houver reservas.
  - **Idempotência parcial via guardas de estado** (igual ao item 2; dedup real por `eventId`/`processed_messages` fica no item 4): redelivery com reservas RESERVED republica `StockReserved`, com FAILED republica `StockReservationFailed`, sem reprocessar. A aresta `ReserveStock` após um release completo fica para o item 4.
  - **Mensageria/persistência**: `adapter/messaging/in/StockCommandListener` (`@KafkaListener` em `inventory.commands`, converte o payload `Map`→`StockCommand` via `ObjectMapper`, roteia por `envelope.type()`); `InventoryEventPublisherImpl` sobre o `EventPublisher` genérico; `Topics`/`MessageType`/`StockReplyPayload`. Flyway `V2__create_stock_reservations.sql`; `StockReservationEntity` + `StockReservationEntityMapper` (MapStruct) + `JpaDatabase` + `RepositoryImpl`. `inventory.commands` **não** é declarado aqui (produtor é dono = order).
  - **Verificado:** `:inventory-service:check` **verde** — unit + integração Testcontainers (`StockCommandFlowIntegrationTest`: reserva-feliz / insuficiente / release ponta-a-ponta, afirmando resposta **e** efeitos no banco; `StockReservationRepositoryImplIntegrationTest`); **Pitest 97%** (33/34, test strength **100%**; os use cases são alvos). JaCoCo passa.
- [x] ~~Idempotência (`processed_messages`) + DLT por consumidor~~
  - **Idempotência (padrão INBOX) nos dois serviços do núcleo** (`order` e `inventory`, duplicado sem módulo compartilhado): novo `adapter/messaging/in/IdempotentMessageProcessor` (`@Transactional processOnce(eventId, consumer, handler)`) grava o marcador do `eventId` **na mesma transação** do efeito de negócio — se o handler falha, o rollback **não** registra o marcador e a mensagem volta para retry/DLT. A PK composta `(event_id, consumer)` é o backstop de corrida. Tabela `processed_messages` via Flyway (`order V2`, `inventory V3`), entidade Lombok com `@IdClass`, `JpaRepository`, `*RepositoryImpl @Repository @Transactional` e porta `application/port/out/ProcessedMessage(Repository)` — espelhando o trio de persistência do `StockReservation`. Discriminador `consumer` **por listener** (`order.stock-reply`/`order.payment-reply`/`inventory.stock-command`) em `adapter/messaging/Consumers`.
  - **Complementa (não substitui) as guardas de estado:** o dedup por `eventId` barra a **redelivery do mesmo evento**; as guardas de estado de domínio continuam absorvendo um comando/resposta **re-emitido com novo `eventId`** (o produtor gera um `eventId` novo a cada publish). O `order-service` passa a processar cada mensagem numa **transação única** (os `OrderRepositoryImpl.save` se juntam à tx do `processOnce`); o `catch (InvalidOrderStateException)` do coordinator segue como no-op idempotente (commita só o marcador).
  - **DLT por consumidor (`<topic>.DLT`):** `adapter/config/KafkaErrorHandlingConfig` declara um único `DefaultErrorHandler` (auto-associado pelo Boot à factory padrão — sem factory custom) com `DeadLetterPublishingRecoverer` + `FixedBackOff(1s, 3)`; `DeserializationException` (já fatal) e `IllegalArgumentException` (UUID/`convertValue` malformado) vão **direto** ao DLT. Template de DLT **dedicado** (não exposto como bean, p/ não suprimir o `KafkaTemplate`/`ProducerFactory` auto-config) com `DelegatingByTypeSerializer` (`byte[]`→`ByteArraySerializer`, `EventEnvelope`→`JacksonJsonSerializer().noTypeInfo()`). **Dois ajustes de versão (Spring Kafka 4.0):** resolver de destino **explícito** forçando o sufixo `.DLT` (o padrão do SK4 virou `-dlt`); e remoção das props `spring.json.*` herdadas do produtor (o `JacksonJsonSerializer` não pode ser configurado por setter **e** por properties). Consumidores passam a usar `ErrorHandlingDeserializer` (main+test) p/ poison-pill ir ao DLT em vez de travar a partição. Tópicos `*.DLT` declarados pelo **consumidor** (order: `inventory.events.DLT` + `payment.events.DLT`; inventory: `inventory.commands.DLT`).
  - **Verificado:** `:order-service:check` + `:inventory-service:check` **verdes** — unit (`IdempotentMessageProcessorTest`, `ProcessedMessageIdTest`, listeners atualizados c/ caso duplicado) + integração Testcontainers `StockCommandResilienceIntegrationTest` (redelivery reserva uma vez / payload malformado→DLT / poison-pill→DLT via `ErrorHandlingDeserializer` com captura em `byte[]`) e `StockReplyResilienceIntegrationTest` (redelivery avança a saga uma vez / `orderId` malformado→DLT). **JaCoCo** passa; **Pitest** segue 100% (order) / 97% (inventory), test strength 100% — sem mutações novas (código novo fora do alvo `application.*`/`web.*Controller`).
  - **Fora deste item:** consumidores de `payment-service`/`notification-service` (Fase 3); atomicidade real DB+broker (**Transactional Outbox**, FUTURE).
- **Critério:** pedido feliz **confirma**; sem estoque **rejeita**; pagamento recusado **cancela com compensação**.

## 💳 Fase 3 — Payment & Notification
- [x] ~~`payment-service`: PSP mock com cenários de aprovação/recusa controláveis~~
  - **Hexagonal horizontal** espelhando o `inventory-service` (mesma fundação Kafka/idempotência/DLT do núcleo). **Domínio**: record imutável `Payment(id, orderId, amount, status, providerRef)` (§7) com factories `authorized(...)`→AUTHORIZED e `failed(...)`→FAILED e transição `refund()` (AUTHORIZED→REFUNDED; ilegal lança `IllegalPaymentStateException`); enum `PaymentStatus {AUTHORIZED, FAILED, REFUNDED}`; `DomainException`/`PaymentNotFoundException`. Domínio sem Spring/JPA.
  - **PSP mock controlável** (o coração do item): porta de saída `PaymentGateway` (`authorize`/`refund` → `PaymentDecision`) com adapter `adapter/psp/MockPaymentGateway` dirigido por `@ConfigurationProperties("shopflow.payment.psp")` (`PspProperties`): `mode` = `APPROVE_ALL` / `DECLINE_ALL` / `DECLINE_ABOVE_THRESHOLD` (default) + `decline-threshold` (default `1000.00`). No modo threshold o `amount` do comando decide o resultado (`amount >= threshold` recusa), então **uma única instância cobre aprovação e recusa** nos testes. Decisão **determinística** (sem aleatoriedade) e todo retorno traz um `providerRef`. Os defaults vivem no Java (não só no yaml) p/ o yaml de teste **sombrear** sem quebrar o mock.
  - **Aplicação**: in-ports `AuthorizePaymentUseCase`/`RefundPaymentUseCase` (`execute(PaymentCommand)`) + `GetPaymentUseCase`; out-ports `PaymentRepository`, `PaymentEventPublisher` (porta de domínio espelhando o `InventoryEventPublisher`), `PaymentGateway`/`PaymentDecision`, `ProcessedMessage(Repository)`; viewmodel `PaymentCommand(orderId, customerId, amount)` (espelha o `ProcessPaymentPayload` do order). Impls `@Service`/`@Transactional` que **publicam a própria resposta** (listener fino).
  - **Authorize**: sem pagamento prévio chama o PSP → grava `Payment` AUTHORIZED/FAILED + publica `PaymentAuthorized`/`PaymentFailed`. **Refund** (compensação): usa o pagamento persistido como fonte da verdade (AUTHORIZED→refunda no PSP, marca REFUNDED, `PaymentRefunded`); FAILED/inexistente é no-op. **Idempotência por guarda de estado** (igual ao núcleo): redelivery com pagamento AUTHORIZED republica `PaymentAuthorized`, com FAILED republica `PaymentFailed`, com REFUNDED ignora; refund redelivered republica `PaymentRefunded`.
  - **Mensageria/persistência**: `adapter/messaging/in/PaymentCommandListener` (`@KafkaListener` em `payment.commands`, converte o payload `Map`→`PaymentCommand` via `ObjectMapper`, roteia por `envelope.type()`) sob o `IdempotentMessageProcessor` (`processed_messages`, consumer `payment.payment-command`); `PaymentEventPublisherImpl` sobre o `EventPublisher`/`KafkaEventPublisher` genérico; `Topics`/`MessageType`/`Consumers`/`PaymentReplyPayload`. `KafkaErrorHandlingConfig` (DLT `payment.commands.DLT` via `DeadLetterPublishingRecoverer` + `FixedBackOff(1s,3)`, `IllegalArgumentException` não-retryable). **payment-service é dono de `payment.events` + `payment.commands.DLT`** (o order já declara `payment.commands` + `payment.events.DLT` e já consome `PaymentAuthorized`/`PaymentFailed`) → **zero mudança no order-service** no fluxo de autorização. Flyway `V1__create_payments.sql` (`order_id` UNIQUE) + `V2__create_processed_messages.sql`; `PaymentEntity`/`PaymentEntityMapper` (MapStruct) + `JpaDatabase` + `RepositoryImpl`.
  - **Web**: `GET /payments/{orderId}` → 200 `PaymentResponse` / 404, `GlobalExceptionHandler` + `ProblemType` (RFC 7807, `PAYMENT_NOT_FOUND`) + Swagger — paridade com o `GET /stock`.
  - **Verificado:** `:payment-service:check` **verde** — unit (domínio/use cases/gateway/listener/publishers/mapper/controller/ProblemType) + integração Testcontainers (`PaymentCommandFlowIntegrationTest`: aprova `amount<1000` / recusa `amount>=1000` / refund ponta-a-ponta, afirmando resposta **e** pagamento persistido; `PaymentCommandResilienceIntegrationTest`: redelivery autoriza uma vez / payload malformado→DLT / poison-pill→DLT; `PaymentRepositoryImplIntegrationTest`). **JaCoCo LINE 0.98 / BRANCH 0.94**; **Pitest 19/19 (100%)**, test strength **100%**. Object Mother + `@DisplayName` Given/When/Then.
  - **Fora deste item:** `notification-service` (**item 2**); **emissor de `RefundPayment` + consumo de `PaymentRefunded` no order-service** — o refund nasce completo no payment mas a saga atual não o dispara, então é exercitado por teste (não ponta-a-ponta); atomicidade real DB+broker (**Transactional Outbox**, FUTURE).
- [x] ~~`notification-service`: consome `order.events` e registra notificação (mock/log)~~
  - **Consumidor terminal de `order.events`** (espelha a fundação Kafka/idempotência/DLT do núcleo e do payment). **Domínio enxuto** (CLAUDE.md permite layout lean, mas mantida a convenção hexagonal horizontal): record imutável `Notification(id, orderId, customerId, type, message, createdAt)` com factory `create(...)` (gera `id`) e enum `NotificationType {ORDER_CREATED, ORDER_CONFIRMED, ORDER_REJECTED, ORDER_CANCELLED}` que **renderiza a mensagem** por tipo. Sem Spring/JPA no domínio.
  - **Notifica nos quatro tipos** de `order.events` (o critério confirm/cancel é subconjunto): `OrderCreated`→"recebido", `OrderConfirmed`→"confirmado (com total)", `OrderRejected`→"recusado: sem estoque", `OrderCancelled`→"cancelado: pagamento recusado". O "envio" é **mock por log**; a notificação é **persistida** para inspeção.
  - **Aplicação**: in-ports `RecordNotificationUseCase` (`execute(NotificationCommand)`) + leitura `ListNotificationsUseCase`/`GetNotificationsByOrderUseCase`; out-ports `NotificationRepository` (`save`/`findAll`/`findByOrderId`) e `ProcessedMessage(Repository)`; viewmodel `NotificationCommand(orderId, customerId, type, totalAmount)`. Impls `@Service` (`@Transactional` na escrita, `readOnly` nas leituras).
  - **Mensageria/persistência**: `adapter/messaging/in/OrderEventListener` (`@KafkaListener` em `order.events`, mapeia `envelope.type()`→`NotificationType`, converte o payload `Map`→`OrderEventPayload` via `ObjectMapper`, tipo desconhecido vira log+skip) sob o `IdempotentMessageProcessor` (`processed_messages`, consumer `notification.order-event`); `Topics`/`MessageType`/`Consumers`/`EventEnvelope`/`payload.OrderEventPayload` duplicados (sem módulo compartilhado). `KafkaErrorHandlingConfig` (DLT `order.events.DLT` via `DeadLetterPublishingRecoverer` + `FixedBackOff(1s,3)`, `IllegalArgumentException` não-retryable). **notification-service é dono de `order.events.DLT`** (o order já declara `order.events`); **zero mudança no order-service**. Flyway `V1__create_notifications.sql` (índice por `order_id`) + `V2__create_processed_messages.sql`; `NotificationEntity` + `NotificationEntityMapper` (MapStruct) + `JpaDatabase` + `RepositoryImpl`.
  - **Web**: `GET /notifications` (lista, mais recentes primeiro) e `GET /notifications/{orderId}` (lista do pedido, vazia se não houver) → `NotificationResponse`, `GlobalExceptionHandler` + `ProblemType` (RFC 7807) + Swagger — paridade com `GET /stock`/`GET /payments`.
  - **Verificado:** `:notification-service:check` **verde** — unit (domínio/use cases/listener/idempotência/mapper/controller/ProblemType) + integração Testcontainers (`NotificationFlowIntegrationTest`: os 4 tipos publicados em `order.events` viram notificação persistida; `NotificationResilienceIntegrationTest`: redelivery registra uma vez / payload malformado→DLT / poison-pill→DLT; `NotificationRepositoryImplIntegrationTest`). **JaCoCo** (LINE ≥ 0.85 / BRANCH ≥ 0.75) passa; **Pitest 4/4 (100%)**, test strength **100%**. Object Mother + `@DisplayName` Given/When/Then.
  - **Fora deste item:** atomicidade real DB+broker (**Transactional Outbox**, FUTURE); canais de envio reais (e-mail/SMS) seguem mockados por log.
- **Critério:** ~~notificação registrada em confirm e cancel.~~ ✅ **Fase 3 concluída.**

## ✅ Fase 4 — Qualidade
- [x] ~~Testes com **Testcontainers** (Kafka + PostgreSQL) cobrindo happy path + compensação~~
  - **Novo módulo `integration-tests`** (subprojeto Gradle só de teste, sem `src/main`/`bootJar`) com `OrderSagaEndToEndIntegrationTest`: um teste **ponta-a-ponta real** que sobe os **quatro serviços** (`order`/`inventory`/`payment`/`notification`) como contextos Spring independentes no mesmo JVM contra **um Kafka** + **um PostgreSQL** compartilhados (um banco por serviço). Nada é simulado — o pedido entra por **REST** (`POST /orders`) e o fluxo é observado pelos repositórios de cada serviço.
  - **Cenários (3):** happy path → `CONFIRMED` (estoque 98/2, pagamento `AUTHORIZED`, notificação `ORDER_CONFIRMED`); pagamento acima do threshold do PSP → `PaymentFailed` → **compensação** `ReleaseStock` → `CANCELLED` (estoque restaurado 100/0, pagamento `FAILED`, notificação `ORDER_CANCELLED`); estoque insuficiente → `REJECTED` (sem pagamento, estoque intacto, notificação `ORDER_REJECTED`).
  - **Decisões:** cada `@SpringBootApplication` faz component/entity-scan só do seu pacote → os 4 contextos não vazam beans/entidades. Colisões de classpath resolvidas por contexto: `--spring.config.location=classpath:/e2e/<svc>.yaml` (em vez dos 4 `application.yaml` mesclados) e **Flyway desligado + `ddl-auto=create-drop`** (as 4 migrações `db/migration` têm versões repetidas e não convivem num classpath; o schema Flyway real já é validado pelos testes por serviço). Tópicos **pré-criados com 3 partições** antes de subir os contextos (senão o primeiro consumidor auto-cria com 1 partição e a saga trava).
  - **Verificado:** `:integration-tests:integrationTest` **verde** (3/3) e **`./gradlew check` verde** em todos os módulos (critério da fase). Os testes por serviço existentes (que simulam os vizinhos) permanecem; o comentário defasado do `OrderSagaIntegrationTest` foi atualizado.
- [x] ~~Padrão **Object Mother** + `@DisplayName` Given/When/Then~~
  - **Auditoria** da suíte inteira (263 testes, 6 módulos + integration-tests): o padrão já estava ~95% aplicado — 10/10 Object Mothers conformes (final class, ctor privado, factories estáticas), **zero builders**, maioria dos `@DisplayName` em Given/When/Then. Este item **fechou os desvios** restantes para a convenção ficar uniforme.
  - **`@DisplayName`:** reescritos os 4 nomes não-GWT do `OrderSagaIntegrationTest` ("Happy path:" / "No stock:" / "Payment refused:" / "Idempotency:" → "Given …, when …, then …") e adicionado `@DisplayName` GWT aos `contextLoads` de `api-gateway` e `discovery-server` (os 2 únicos sem). Resultado: **todo `@Test` tem um `@DisplayName` Given/When/Then**.
  - **Object Mother:** os testes de integração do inventory (`StockCommandFlowIntegrationTest`, `StockCommandResilienceIntegrationTest`) e o `ReleaseStockUseCaseImplTest` passaram a usar `StockItemMother.aStockItemFor(...)` em vez de `new StockItem(...)`. A construção inline nos testes de **modelo de domínio** (`StockItemTest`) e no edge-case do `OrderEntityMapperTest` foi mantida (idiomática). O módulo E2E `integration-tests` mantém fixtures inline (as Mothers vivem em `src/test` e não são alcançáveis de outro módulo) — documentado no Javadoc do teste.
  - **Verificado:** `./gradlew check` **verde** em todos os módulos.
- [x] ~~Gates **JaCoCo + Pitest** e **OpenAPI/Swagger** em todos os serviços~~
  - **Gate de Pitest ligado ao `check`** (uma única edição na raiz, dentro do `plugins.withId("info.solidsoft.pitest")`): `check` passa a depender de `pitest` (com `mustRunAfter(integrationTest)` → o `check` flui unit → integração → mutação). Antes o `pitest` era uma task órfã (rodava só sob demanda), contradizendo o **CHALLENGE §12** ("check com gates JaCoCo **e** Pitest"). O **JaCoCo** já estava ligado ao `check` em todos os subprojetos (BUNDLE LINE ≥ 0.85 / BRANCH ≥ 0.75) — sem mudança.
  - **Escopo dos gates:** Pitest fica nos **4 serviços de domínio** (os únicos que aplicam o plugin + declaram `targetClasses`). **api-gateway** e **discovery-server** ficam de fora — só têm classe `@SpringBootApplication`/`@Configuration`/`RouteLocator` (zero alvo de mutação); sua config é coberta pelo JaCoCo via o `contextLoads`. Decisão **documentada em comentário** na raiz. O grafo do `check` foi conferido por `--dry-run` (pitest presente nos 4, ausente em gateway/discovery).
  - **OpenAPI padronizado nos 4 serviços de domínio:** novo `adapter/config/OpenApiConfig` (bean `OpenAPI` com **título/versão/descrição** por serviço — corrige o "OpenAPI definition" default no Swagger UI); **`@Schema`** (description + example) em **todos os 5 response DTOs** (antes só os request DTOs do order tinham); **`@Parameter`** nos path vars dos 4 controllers + **`@RequestBody`** (swagger) no `POST /orders`; bloco **`springdoc`** no `application.yaml` de cada serviço (paths `/v3/api-docs` e `/swagger-ui.html` + `operations-sorter`/`tags-sorter`). Os controllers já tinham `@Tag`/`@Operation`/`@ApiResponses`. Teste unitário **`OpenApiConfigTest`** por serviço (garante cobertura do bean **sem Docker**, já que `adapter.config` entra no JaCoCo). **Gateway**: webflux sem rotas reais ainda + o CHALLENGE pede "Swagger **por serviço**" (sem agregação) → fora de escopo; **discovery** é só Eureka → fora de escopo.
  - **Endurecimento de teste flaky (descoberto ao rodar o `check` completo):** sob a carga total do `check` (todos os módulos subindo Testcontainers + pitest em paralelo) o `PaymentCommandFlowIntegrationTest.refund` falhava de forma intermitente — corrida assíncrona **pré-existente**, não relacionada a este item: o teste observava o evento de resposta no Kafka e lia o banco **uma vez**, mas o publish acontece **dentro da `@Transactional`, antes do commit** (o send do Kafka não é atômico com o commit do DB), então a leitura pegava o estado antigo. Corrigido trocando a leitura única por um **poll da fonte da verdade** (helper que consulta o repositório até o estado esperado, com timeout). A auditoria dos irmãos achou o mesmo padrão em `StockCommandFlowIntegrationTest`, `StockCommandResilienceIntegrationTest`, `OrderSagaIntegrationTest` e `StockReplyResilienceIntegrationTest` — todos endurecidos pelo mesmo gate; os de notification e o E2E já faziam poll (sem mudança).
  - **Verificado:** `./gradlew check` **verde** (`BUILD SUCCESSFUL`, todos os módulos) — unit + integração Testcontainers + gates **JaCoCo e Pitest** rodando dentro do `check` (4 tasks `pitest` no grafo: order/inventory/payment/notification; **`Ran N > 0`** com kill 100% nos três e 97% no inventory; gateway/discovery sem pitest). Critério da fase atendido.
- **Critério:** ~~`./gradlew check` verde com testes de integração.~~ ✅ **Fase 4 concluída.**

## 📈 Fase 5 — Observabilidade
- [x] ~~Actuator + Prometheus + Grafana (dashboards)~~
  - **Métricas via Actuator/Micrometer → Prometheus nos 6 serviços:** cada `build.gradle.kts` ganhou `runtimeOnly("io.micrometer:micrometer-registry-prometheus")` (versão pela BOM do Boot) e cada `application.yaml` um bloco `management` expondo `health,info,prometheus` (o `api-gateway` preserva o `gateway`), `endpoint.health.show-details: always` e a **common tag** `application: ${spring.application.name}` — aplicada a **todas** as métricas (JVM/HTTP/Kafka/JDBC + as de negócio). Endpoint `/actuator/prometheus` por serviço. Os `application.yaml` de `src/test` **não** foram tocados (sombreiam o de `main`; o bean `MeterRegistry` existe de qualquer forma, então coleta/injeção funcionam nos testes/integração).
  - **Métricas de negócio da saga** (counters, namespace `shopflow.*`, viram `*_total` no Prometheus): `MeterRegistry` injetado por construtor nos use cases (`application.usecase`; o domínio segue sem Micrometer). `order`: `shopflow.orders.placed` (`PlaceOrderUseCaseImpl`) + `shopflow.saga.outcome{outcome=confirmed|rejected|cancelled}` (`OrderSagaCoordinator`, nos 3 ramos terminais). `payment`: `shopflow.payments.outcome{outcome=authorized|failed|refunded}` (`Authorize`/`RefundPaymentUseCaseImpl`). `inventory`: `shopflow.reservations.outcome{outcome=reserved|failed|released}` (`Reserve`/`ReleaseStockUseCaseImpl`). **Incrementa só na transição de negócio real** — nunca nos ramos de idempotência/republish (a redelivery do Kafka não conta duas vezes; multi-item conta 1×). **Sem Timer de duração da saga**: cross-async não cabe num `Timer.Sample` em memória — latência ponta-a-ponta é responsabilidade do tracing (item 2). `notification-service` fica fora das métricas de negócio (layout lean, não é alvo de Pitest), mas **recebe** o registry p/ ser raspado (JVM/HTTP).
  - **Infra (containers) no `docker-compose.yml`:** `prometheus` (`prom/prometheus:v3.1.0`, `:9090`) + `grafana` (`grafana/grafana:11.4.0`, `:3000`, admin via `.env`) na rede `shopflow`. Como os 6 serviços Spring rodam no **host** (`bootRun`), o Prometheus raspa via `host.docker.internal` (`extra_hosts: host-gateway`). `infra/prometheus.yml` declara **um job por serviço** (`metrics_path: /actuator/prometheus`). Provisioning do Grafana (subpastas montadas individualmente p/ não sobrepor as default e evitar erros benignos de log): **datasource** Prometheus (`uid: prometheus`) + **provider de dashboards** carregando `infra/grafana/dashboards/*.json` → **`ShopFlow Overview`** (up/down, taxa/latência p95 HTTP, heap JVM, consumer lag Kafka, funil de pedidos/pagamentos/reservas) e **`ShopFlow JVM (per service)`** (variável `$application`: memória por área/pool, GC, threads, classes, CPU). Novas vars em `.env`/`.env.example` (`PROMETHEUS_PORT`, `GRAFANA_PORT`, `GRAFANA_ADMIN_USER/PASSWORD`).
  - **Testes (gate-safe):** os `*UseCaseImplTest`/`OrderSagaCoordinatorTest` dos 3 serviços passaram a construir o use case com um `SimpleMeterRegistry` **real** (trocando `@InjectMocks` por um `@BeforeEach` que injeta os mocks + o registry) e afirmam o counter no ramo positivo (`count() == 1.0`, selecionando por tag) e a **ausência** do meter nos ramos negativos/idempotência — o que mata as mutações `VoidMethodCall` dos `increment()` no Pitest. Object Mother + `@DisplayName` Given/When/Then preservados; sem inflar a suíte (asserções pegam carona nos testes de ramo existentes).
  - **Verificado:** `./gradlew check` **verde** em todos os módulos (`BUILD SUCCESSFUL`) — unit + integração Testcontainers (incl. o E2E `integration-tests`) + **JaCoCo** (LINE ≥ 0.85 / BRANCH ≥ 0.75) e **Pitest** nos 4 (mutações dos `increment()` mortas — `VoidMethodCallMutator` 100%; inventory 97%/test strength 100%, demais 100%). Smoke do stack: `docker compose up -d prometheus grafana` sobe ambos; Grafana `:3000` provisiona o datasource e **carrega os 2 dashboards** (API confirmada, sem erros de provisioning); Prometheus `:9090` com os **6 jobs** configurados.
  - **Fora deste item:** **tracing Micrometer → Zipkin com propagação pelo Kafka (item 2)** — só com ele a linha **Critério** da fase fecha; OpenTelemetry/stack LGTM (FUTURE Fase 18); containerização dos serviços Spring; autenticação dos endpoints actuator (Fase 6).
- [x] ~~Tracing Micrometer → Zipkin com **propagação do trace pelo Kafka**~~
  - **Tracer Brave → Zipkin nos 5 serviços do caminho da requisição** (`api-gateway` + `order`/`inventory`/`payment`/`notification`): cada `build.gradle.kts` ganhou o **starter único** `org.springframework.boot:spring-boot-starter-zipkin` (Spring Boot 4 — traz `micrometer-tracing-bridge-brave` + reporter Zipkin, versão pela BOM). **`discovery-server` fica de fora** (Eureka, não participa do trace da saga). Escolhido **Brave** (e não OpenTelemetry) porque OTel/stack LGTM é a **FUTURE Fase 18**.
  - **Propagação pelo Kafka por configuração (zero bean/código de produção novo):** nos 4 serviços de domínio, `spring.kafka.template.observation-enabled: true` (o producer injeta o header W3C `traceparent` no record) e `spring.kafka.listener.observation-enabled: true` (o consumer continua o trace a partir do header), sobre os beans **auto-configurados** — sem declarar `KafkaTemplate`/`ConcurrentKafkaListenerContainerFactory` à mão. Como não há classe de produção nova, os gates **JaCoCo/Pitest** ficam intactos. O `deadLetterTemplate` manual (caminho de erro) segue **sem** observação, de propósito.
  - **Export p/ Zipkin:** `management.tracing.sampling.probability: 1.0` (dev/portfólio: traça toda saga) + `management.tracing.export.zipkin.endpoint: http://localhost:9411/api/v2/spans` nos 5 serviços — atenção às **propriedades renomeadas no Boot 4** (`management.zipkin.tracing.*` → `management.tracing.export.zipkin.*`). O `api-gateway` gera o span de entrada (raiz do trace) e propaga por HTTP até o `order-service`.
  - **Infra:** container `zipkin` (`openzipkin/zipkin:3.6.0`, `:9411`, storage em memória) no `docker-compose.yml` (rede `shopflow`) + nova var `ZIPKIN_PORT` em `.env`/`.env.example`. Os serviços rodam no host (`bootRun`) e empurram spans p/ `localhost:9411`.
  - **Teste (gate-safe):** novo IT **`KafkaTracePropagationIntegrationTest`** (order-service, espelhando `KafkaEnvelopeRoundTripIntegrationTest`) afirma que um envelope publicado pelo `EventPublisher` chega ao consumidor **com o header `traceparent`** no formato W3C — prova objetiva da "propagação do trace pelo Kafka". Nos `application.yaml` de `src/test` e nos `e2e/*.yaml` o **export ao Zipkin é desligado** (`management.tracing.export.zipkin.enabled: false`, sem backend em CI); o order-service de teste habilita a observação Kafka p/ o IT ser determinístico. Guia operacional (subir, gerar saga, ler o trace) na nova **seção 6 do `OBSERVABILITY.md`**.
  - **Verificado:** `./gradlew check` **verde** (`BUILD SUCCESSFUL`, todos os módulos) — unit + integração Testcontainers (incl. o novo `KafkaTracePropagationIntegrationTest`: 1 teste, 0 falhas) + **JaCoCo** (LINE ≥ 0.85 / BRANCH ≥ 0.75) e **Pitest** nos 4 (sem código de produção novo → mutações inalteradas). **Smoke ponta-a-ponta real:** `docker compose up -d` + os 4 serviços via `bootRun`; um `POST /orders` (CONFIRMED) gerou no Zipkin **um único trace** (`traceId` com **13 spans nos 4 serviços**, todos sob o mesmo id) encadeando `http post /orders` → `order.events`/`inventory.commands send` → `inventory.commands process` → `inventory.events send` → `order` consome → `payment.commands send` → `payment` processa/autoriza → `order` consome → `order.events send` (OrderConfirmed) → `notification` consome — a latência ponta-a-ponta da saga que não cabia num `Timer` (Fase 5, item 1).
  - **Fora deste item:** datasource Zipkin no Grafana / correlação métricas↔traces / OpenTelemetry / stack LGTM → **FUTURE Fase 18**; containerização dos serviços Spring; autenticação dos endpoints actuator (Fase 6).
- **Critério:** ~~trace de uma saga ponta-a-ponta no Zipkin; dashboard no Grafana.~~ ✅ **Fase 5 concluída.**

## 🔐 Fase 6 — Segurança
- [x] ~~Keycloak (realm `shopflow`, roles `CUSTOMER`/`ADMIN`)~~
  - **Item puramente de infraestrutura** (zero código de produção; a validação do JWT no gateway/serviços é o **item 2**). Novo serviço **`keycloak`** no `docker-compose.yml` (`quay.io/keycloak/keycloak:26.6.3` — linha 26.6.x do CHALLENGE §10, na rede `shopflow`), em modo **`start-dev --import-realm`** (H2 em memória; o realm é **re-importado a cada start**). Porta **host 8088 → container 8080** (espelha o `kafka-ui` `8085:8080`; §13 pede `:8088`); como os serviços Spring rodam no **host**, o issuer derivado fica consistente em `http://localhost:8088/realms/shopflow`. Admin bootstrap do realm **master** via `KC_BOOTSTRAP_ADMIN_USERNAME/PASSWORD` (**renomeados na v26**, antes `KEYCLOAK_ADMIN*`); novas vars `KEYCLOAK_PORT`/`KEYCLOAK_ADMIN`/`KEYCLOAK_ADMIN_PASSWORD` em `.env`/`.env.example`. Sem healthcheck (igual aos outros 5 serviços de infra).
  - **`infra/keycloak/realm-export.json`** (a fonte da verdade do realm): realm `shopflow` (`sslRequired: none` p/ dev sobre HTTP) com as **roles** `CUSTOMER`/`ADMIN`, **1 client confidencial** `shopflow-gateway` (`directAccessGrantsEnabled: true` — fluxo *password* para Postman/`curl`/testes; secret de dev `shopflow-gateway-secret`, standard/implicit flow off) e **2 usuários de teste** (`customer`→CUSTOMER, `manager`→ADMIN). O client + usuários deixam o item **verificável** e já preparam o item 2.
  - **Verificado (ponta a ponta, container real):** `docker compose up -d keycloak` → log **`Realm 'shopflow' imported` / `Import finished successfully`**, Keycloak 26.6.3 *Listening on http://0.0.0.0:8080*. Token via *password grant* p/ os dois usuários → **HTTP 200** com `access_token` cujo `realm_access.roles` traz **`["CUSTOMER"]`** (customer) e **`["ADMIN"]`** (manager), `iss=http://localhost:8088/realms/shopflow`, `azp=shopflow-gateway`. `/.well-known/openid-configuration` responde 200 (o que o item 2 vai consumir). Guia operacional novo em **`doc/SECURITY.md`** (subir, console, obter/inspecionar JWT, troubleshooting).
  - **Fora deste item:** proteger os endpoints de fato — **gateway OAuth2 + serviços como resource servers validando o `Bearer` JWT** (item 2), que fecha o critério da fase; rate limit do gateway; containerização dos serviços Spring.
- [x] ~~Gateway OAuth2 + serviços como resource servers (JWT)~~
  - **Os 5 serviços do caminho da requisição viram OAuth2 resource servers** (starter Boot 4 **renomeado**: `spring-boot-starter-security-oauth2-resource-server`; teste com `...-resource-server-test`). Cada `application.yaml` ganhou `spring.security.oauth2.resourceserver.jwt.issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8088/realms/shopflow}` (descoberta OIDC + validação de assinatura/issuer via JWKS). Nos `application.yaml` de **teste** usa-se `jwk-set-uri` (decoder **lazy**) p/ os contextos `@SpringBootTest` subirem **sem Keycloak**.
  - **`SecurityConfig` por serviço** (`adapter/config`, `@EnableWebSecurity`, **`@ConditionalOnWebApplication(SERVLET)`** — pulado nos contextos `web-application-type: none` do E2E), stateless + `csrf` off. **Política role→endpoint** (a doc não prescrevia — decisão de design): `POST /orders`→**CUSTOMER**; `GET /stock/**`→**ADMIN**; `GET /orders|payments|notifications/**`→autenticado; `/actuator/**` + Swagger → **permitAll**. Roles do Keycloak vêm em `realm_access.roles` → mapeadas p/ `ROLE_*` por um `KeycloakRealmRoleConverter` (em order/inventory, que usam roles; payment/notification só exigem autenticação).
  - **Gateway**: `SecurityConfig` reativo (`@EnableWebFluxSecurity`) exige JWT em toda rota de negócio e **encaminha** o `Authorization` (default do Spring Cloud Gateway) — autentica na borda, **roles ficam nos serviços** (defense in depth). `GatewayRoutesConfig` trocou a rota *dummy* por **rotas reais** `lb://` (`/orders`→order, `/stock`→inventory, `/payments`→payment, `/notifications`→notification, via Eureka).
  - **Testes**: os 20 `@WebMvcTest` agora autenticam via `spring-security-test` (`jwt().authorities(...)`) + `@MockitoBean JwtDecoder`, com casos novos **401 (sem token)** e **403 (role errada)**; `KeycloakRealmRoleConverterTest` (order/inventory) cobre o mapeamento; novo `ApiGatewaySecurityTest` (`WebTestClient`) afirma 401 sem token e actuator aberto; o **E2E `OrderSagaEndToEndIntegrationTest` sobe um Keycloak real (Testcontainers `dasniko/testcontainers-keycloak`)**, importa o realm, injeta o `issuer-uri` dinâmico no order-service e manda um **JWT genuíno** no `POST /orders` (auth real ponta a ponta). `SecurityConfig`/conversor ficam em `adapter.config` → **fora do alvo do Pitest** (cobertos por JaCoCo).
  - **Verificado:** `./gradlew check` **verde** (`BUILD SUCCESSFUL`, todos os módulos) — unit + integração Testcontainers (incl. o E2E com Keycloak real: 3 cenários da saga com Bearer token) + **JaCoCo** (LINE ≥ 0.85 / BRANCH ≥ 0.75) e **Pitest** nos 4. Guia `doc/SECURITY.md` estendido (config resource server/gateway, tabela role→endpoint, exemplos `curl` com Bearer e respostas 401/403, ordem de subida).
  - **Fora deste item:** rate limit do gateway (não é item da Fase 6); derivar `customerId` do `sub` do token (re-modelagem de identidade — futuro); autenticação dos endpoints actuator (quebraria o scrape do Prometheus); containerização dos serviços Spring.
- **Critério:** ~~`POST /orders` exige JWT válido.~~ ✅ **Fase 6 concluída.**

## 🚀 Fase 7 — CI/CD
- [x] ~~GitHub Actions: build + `test` + `integrationTest` (Testcontainers)~~
  - **Workflow `.github/workflows/ci.yml`** (caminho previsto no CHALLENGE §9): job único em **`ubuntu-latest`** que roda **`./gradlew build`** — compila + unit (`test`) + **`integrationTest`** (Testcontainers) + gates **JaCoCo/Pitest** + `assemble`. Como o `ubuntu-latest` já traz Docker, o Testcontainers sobe Kafka/PostgreSQL **e o Keycloak do E2E** como containers descartáveis (sem bloco `services:`). Passos: `actions/checkout@v4` → `actions/setup-java@v4` (**temurin 21**, casa com o toolchain do build) → `gradle/actions/setup-gradle@v4` (cache de deps/wrapper) → `./gradlew build --no-daemon --stacktrace`.
  - **Triggers**: `pull_request` → `main` (o critério da fase) + `push` → `main` (protege o histórico pós-merge) + `workflow_dispatch` (manual). **`permissions: contents: read`** (mínimo; escrita no GHCR é o item 2) e **`concurrency`** com `cancel-in-progress` (cancela runs supersedidos do mesmo PR).
  - **Fix necessário p/ runner Linux:** o `gradlew` estava com modo `100644` no git; marcado **`100755`** (`git update-index --chmod=+x`) para o `./gradlew` executar no runner.
  - **Verificado (local):** YAML válido (parser) e o comando exato do CI, **`./gradlew build`, verde** (`BUILD SUCCESSFUL`, todos os módulos — unit + integração Testcontainers + JaCoCo + Pitest + boot jars). O **"pipeline verde no PR"** só é observável **após publicar no GitHub** (o repo é local-only): ao abrir um PR de uma branch → `main`, o Actions roda o workflow do branch do PR.
  - **Fora deste item:** build/push de imagens p/ o **GHCR** no merge p/ `main` (**item 2**); README + badge de status (**Fase 8**); branch protection / required checks (config no GitHub).
- [x] ~~Build e push das imagens para o **GHCR** no merge para `main`~~
  - **`Dockerfile` por serviço** (6, caminho previsto no CHALLENGE §9 — `api-gateway`/`discovery-server`/`order`/`inventory`/`payment`/`notification`; `integration-tests` não tem bootJar → fora). **Imagem fina** (escolha do usuário): base **`eclipse-temurin:21-jre`**, usuário **não-root** `spring`, `COPY build/libs/app.jar` + `ENTRYPOINT java -jar`. Difere só no `EXPOSE` (8080/8761/8081/8082/8083/8084). Sem multi-stage: o jar é buildado pelo Gradle (cache) antes do `docker build`.
  - **Nome previsível do bootJar:** o plugin Spring Boot emite dois artefatos (`bootJar` + `*-plain.jar`), então `COPY *.jar` seria ambíguo. Fixado **`archiveFileName = "app.jar"`** no `build.gradle.kts` raiz (bloco novo `plugins.withId("org.springframework.boot")`), **sem** desabilitar o `jar` plain — o módulo `integration-tests` consome os serviços via `project(":…")` (artefato plain), que continua intacto.
  - **Workflow `.github/workflows/publish-images.yml`** (separado do `ci.yml`): trigger **`push` → `main`** (= "no merge para main") + `workflow_dispatch`; **`permissions: { contents: read, packages: write }`**; **matrix** dos 6 serviços, cada job: setup-java 21 + setup-gradle → `./gradlew :<svc>:bootJar` → `docker/login-action` no `ghcr.io` (via `GITHUB_TOKEN`) → `docker/metadata-action` (tags **`latest`** + **`sha-<short>`**, owner em lowercase) → `docker/build-push-action` (`context: <svc>`, `push: true`) → `ghcr.io/<owner>/shopflow-<svc>`.
  - **Verificado (local):** `./gradlew build` **verde** após o rename do bootJar (nada quebrou, incl. o E2E); **imagem buildada e executada** — `docker build` do `order-service` ok e `docker run` mostrou o **banner do Spring Boot 4.0.7** (`Starting OrderServiceApplication … Java 21.0.11 … /app/app.jar started by spring`, Tomcat 8081), parando só na conexão com o DB (esperado sem infra) → prova que a imagem roda como não-root. YAML do workflow válido (matrix com 6). **"Imagens publicadas no merge"** só ocorre **no GitHub** (repo local-only): no push/merge à `main`, o workflow empurra as 6 imagens p/ o GHCR (aparecem em *Packages*).
  - **Fora deste item:** wire do `docker-compose.yml` p/ rodar os serviços a partir das imagens (`compose up --build` do §13 — o modelo segue infra-em-container + serviços via `bootRun`); multi-arch, cosign, SBOM, scanning; Kubernetes/Helm (**FUTURE Fase 14**); README + badge (**Fase 8**).
- **Critério:** ~~pipeline verde no PR; imagens publicadas no merge.~~ ✅ **Fase 7 concluída** (o pipeline e o publish rodam no GitHub após publicar o repo).

## 🎁 Fase 8 — Acabamento de portfólio
- [ ] README com diagramas (mermaid), badges e GIF/asciinema do fluxo
- [ ] Coleção Postman/HTTPie + instruções de uso
- **Critério:** repositório "vendável" e fácil de rodar por terceiros.
